apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lmcache.fullname" . }}-config
  labels:
    {{- include "lmcache.labels" . | nindent 4 }}
data:
  lmcache_config.yaml: |
{{ .Values.lmcache.config | indent 4 }}
